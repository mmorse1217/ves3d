# Flags
ARFLAGS  = -r
CXXFLAGS+= -DPROFILING -DVERBOSE -openmp -fPIC    \
	  -early-template-check -sox -fno-exceptions  #-Wall 

ifeq ($(VES3D_TESTING),yes)
  CXXFLAGS+= -g -O0 -fp-model precise -mp1
else
  CXXFLAGS+= -O3 -finline -DNDEBUG -unroll-agressive -msse3 -malign-double
endif
