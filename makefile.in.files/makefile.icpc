# Flags
ARFLAGS  = -r
CXXFLAGS+= -openmp -fPIC  -early-template-check -sox -fno-exceptions  #-Wall 

ifeq ($(VES3D_TESTING),yes)
  CXXFLAGS+= -g -O0 -fp-model precise -mp1 -debug full -fno-inline -DPROFILING -DVERBOSE
else
#  CXXFLAGS+= -O3 -finline -DPROFILING -unroll-agressive -msse3 -malign-double -DNDEBUG
  CXXFLAGS+=  -O3 -finline  -unroll-agressive -msse3 -malign-double -DNDEBUG
endif
